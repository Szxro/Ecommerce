name: Build & Test
on: 
  push:
    branches: [dev]
env:
  DOTNET_VERSION: ${{secrets.DOTNET_VERSION}}
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Display DOTNET VARIABLE info
        run: echo "Value => $DOTNET_VERSION"
