name: Build & Test
on: 
  push:
    branches:
      - dev
  workflow_dispatch:
env:
  DOTNET_VERSION: ${{secrets.DOTNET_VERSION}}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Display variable information
        run: echo "The value => $DOTNET_VERSION"
  
